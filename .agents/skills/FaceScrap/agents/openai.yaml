interface:
  display_name: "FaceScrap"
  short_description: "Repo-specific patterns and workflows for FaceScrap"
  default_prompt: "Use the FaceScrap repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true